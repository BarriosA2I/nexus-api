# Nexus Assistant Unified - Dependencies

# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
starlette>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# ML/NLP (for local RAG)
scikit-learn>=1.3.0
numpy>=1.24.0

# HTTP client
httpx>=0.25.0
aiohttp>=3.9.0

# Async utilities
anyio>=4.0.0

# Environment
python-dotenv>=1.0.0
python-multipart>=0.0.6

# LLM Providers (Nexus Brain) - REQUIRED FOR CLAUDE
anthropic>=0.39.0

# RAG Components (Research Oracle - Qdrant Cloud configured)
qdrant-client>=1.7.0
# sentence-transformers removed - using OpenAI Embeddings API instead (memory efficient)
redis>=5.0.0

# Event Publishing (Feedback Loop - graceful degradation if RabbitMQ not configured)
aio-pika>=9.0.0

# Scheduling (for Research Oracle)
# APScheduler>=3.10.0
